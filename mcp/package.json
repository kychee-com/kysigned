{
  "name": "kysigned-mcp",
  "version": "0.1.1",
  "description": "MCP server for kysigned — self-verifying e-signatures",
  "type": "module",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/kychee-com/kysigned.git",
    "directory": "mcp"
  },
  "homepage": "https://kysigned.com",
  "keywords": [
    "mcp",
    "e-signature",
    "esignature",
    "dkim",
    "kysigned"
  ],
  "bin": {
    "kysigned-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0"
  }
}
